const db = 'dd';//db 관련 객체
var model = {};

/**
 * 아이디로 사용할 수 있는지 체크한다.
 * @param {String} id : 체크할 아이디 값 
 */
model.id_check = id => {
    /* checking algorithm... */
    return true;
};

module.exports = model;